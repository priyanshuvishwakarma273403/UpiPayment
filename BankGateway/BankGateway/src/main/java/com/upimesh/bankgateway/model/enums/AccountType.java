package com.upimesh.bankgateway.model.enums;

/**
 * AccountType — Types of bank accounts supported for linking.
 */
public enum AccountType {
    SAVINGS,
    CURRENT,
    SALARY,
    NRI,
    JOINT
}
