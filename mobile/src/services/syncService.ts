import { apiClient } from '../lib/apiClient';
import { API_CONFIG } from '../constants/api';

export const syncService = {
  syncOfflineQueue: async (transactions: any[]) => {
    // Boilerplate stub for offline mesh sync
  },
};
