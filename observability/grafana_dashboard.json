{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "id": 1,
      "title": "Transaction Throughput (txns/sec)",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "targets": [{ "expr": "rate(transaction_throughput_total[1m])", "legendFormat": "Throughput" }]
    },
    {
      "id": 2,
      "title": "Kafka Partition Consumer Lag",
      "type": "stat",
      "gridPos": { "h": 8, "w": 6, "x": 12, "y": 0 },
      "targets": [{ "expr": "kafka_consumer_lag_records", "legendFormat": "Consumer Lag" }]
    },
    {
      "id": 3,
      "title": "Service Health Status",
      "type": "stat",
      "gridPos": { "h": 8, "w": 6, "x": 18, "y": 0 },
      "targets": [{ "expr": "service_health_status", "legendFormat": "Health" }]
    },
    {
      "id": 4,
      "title": "Latency Distribution (p95 / p99)",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "targets": [
        { "expr": "histogram_quantile(0.95, rate(risk_evaluation_duration_seconds_bucket[5m]))", "legendFormat": "Risk Latency p95" },
        { "expr": "histogram_quantile(0.95, rate(fraud_detection_duration_seconds_bucket[5m]))", "legendFormat": "Fraud Latency p95" },
        { "expr": "histogram_quantile(0.95, rate(ml_prediction_duration_seconds_bucket[5m]))", "legendFormat": "ML Latency p95" }
      ]
    },
    {
      "id": 5,
      "title": "API HTTP Errors (4xx / 5xx)",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "targets": [{ "expr": "rate(api_errors_total[5m])", "legendFormat": "API Errors" }]
    }
  ],
  "refresh": "5s",
  "schemaVersion": 38,
  "style": "dark",
  "tags": ["sentinelx", "observability", "upi-mesh"],
  "time": { "from": "now-1h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "SentinelX UPI Mesh — Production Observability Dashboard",
  "uid": "sentinelx-obs-v1"
}
